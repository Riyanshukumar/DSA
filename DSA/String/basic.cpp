#include<iostream>
#include<string.h>
using namespace std;

int main(){
   // string str;
    //cin >> str;

   /* int count[26]={0};
    for(int i=0; i<str.length(); i++){
        count[str[i]-'a']++;
    }
    for(int i=0; i<26; i++){
        if(count[i]!=0){
            cout << "the frequency of " << char('a'+i) << " is " << count[i] << endl;
        }
        
    }*/
string s1 = "riyanshu";
int len = s1.length();
cout << len << endl;
    return 0;
    
}